# Example Project Placeholder

Create a real project beside this file using the structure shown in [../project_tree.txt](../project_tree.txt).

Recommended setup:

1. Copy `config_template.yaml` from the skill root into your project as `config.yaml`.
2. Copy `stim_schedule_template.csv` into `02_stim_events/stim_schedule_master.csv` or `.xlsx`.
3. Copy `unit_quality_template.csv` into `01_sorting_info/unit_quality_table.csv` or `.xlsx`.
4. Place `.pl2` files into `00_raw_pl2/`.
